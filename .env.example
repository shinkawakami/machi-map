# ローカル開発は compose.yaml の Postgres をそのまま使う:
#   docker compose up -d
DATABASE_URL="postgresql://machimap:machimap@localhost:5434/machimap?schema=public"

# 本番（Vercel）は Neon などの接続文字列に差し替える:
#   postgresql://USER:PASSWORD@HOST/DB?sslmode=require
